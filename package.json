{
  "name": "dotfiles.download",
  "version": "1.0.0",
  "description": "Landing page for ~(iam) dotfiles",
  "scripts": {
    "dev": "npx eleventy --serve",
    "prebuild": "node scripts/build.js",
    "build": "NODE_ENV=production npx eleventy",
    "deploy:worker": "cd worker && npx wrangler deploy",
    "deploy": "npm run build && npm run deploy:worker"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Jacke/dotfiles.download.git"
  },
  "keywords": ["dotfiles", "zsh", "shell"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0-canary.1"
  }
}
